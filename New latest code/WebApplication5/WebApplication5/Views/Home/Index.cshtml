@model WebApplication5.Models.TechnologyViewModel
@{
    ViewBag.Title = "Export To Excel";
}
<div class="panel">
    <div class="panel-heading">

        @using (Html.BeginForm("ExportToExcel", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.TextBox("ClassID");
            <input type="text" id="ClassID">
            <input type="submit" value="Export" />

        }  
        

     @*<a href="@Url.Action("ExportToExcel")" class="btn btn-primary")>Export</a>
        @Html.Label("Class ID");
        @Html.TextBox("ClassID");
        <input type="text" id="ClassID">
        

        @Html.ActionLink("Export", "ExportToExcel", new {})*@
    </div>
    <div class="panel-body">
        

        @*<table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Project</th>
                    <th>Team Leader</th>
                    <th>Developer</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Technologies)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.Project</td>
                        <td>@item.TeamLeader</td>
                        <td>@item.Developer</td>
                    </tr>
                }
            </tbody>
        </table>*@
    </div>
</div>  
@using (Html.BeginForm("Index", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input type="file" name="file" />
    <input type="submit" value="OK" />
}  
